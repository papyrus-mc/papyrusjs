language: node_js
node_js:
  - 8
before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get -qq install gcc-5.1 g++-5.1
env:
  - CXX=g++-5.1